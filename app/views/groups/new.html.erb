<%- model_class = Group -%>
<%= render 'shared/page_header',
      flash: flash,
      title: 'User Groups',
      description: "Any user groups that you have created are listed in the
        table below. You can create a new group using the <strong>New</strong>
        button shown below the table." %>

<%= render 'form', group: @group %>
